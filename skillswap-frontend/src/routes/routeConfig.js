// Route path constants live in lib/constants.js (ROUTES) so both this
// routing layer and any component that needs to build a link (cards, nav,
// redirects) share a single source of truth. Re-exported here to match the
// project's `routes/` folder convention.
export { ROUTES } from '../lib/constants.js';
