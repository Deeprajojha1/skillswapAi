# Base URL of the SkillSwap backend REST API (must include /api)
VITE_API_URL=http://localhost:5000/api

# Base URL of the SkillSwap backend Socket.IO server (no /api suffix)
VITE_SOCKET_URL=http://localhost:5000
